# you shouldn't commit these into source control
# these are the default directory names, adjust/add to fit your needs
target/
dbt_packages/
logs/
dbt_internal_packages/

# Service account keys (sensitive credentials)
*.json
!package.json
!package-lock.json
!.vscode/extensions.json

# Backups / local-only files that may contain paths or secrets
*.bkp
*.bak
.env
.env.*

# Script outputs
scripts/analytics_row_counts_result.json
